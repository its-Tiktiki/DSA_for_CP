#include <bits/stdc++.h> //includes all the standard libraries use in cp
using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        int l, b;
        cin >> l >> b;
        cout << l * 1LL * b << endl;
    }

    return 0;
}